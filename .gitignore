# Secrets
tg_config.txt

# Logs
*.log

# state.json IS committed (GitHub Actions persists chat settings + getUpdates
# offset between runs); only the temp file is ignored.
state.json.tmp

# Python / Windows
__pycache__/
*.pyc
_popup.ps1
